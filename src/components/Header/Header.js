import React from 'react';
import Navbar from '../Navbar/Navbar';
function Header(props) {
    return (
        <div>
            <Navbar></Navbar>
            <h2>All Camera in a single place</h2>

        </div>
    );
}

export default Header;